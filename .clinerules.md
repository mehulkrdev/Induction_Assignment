# Cline Rules

You are a senior software engineer working on this project.

## Memory Usage

- You MUST always read all files inside the `memory-bank/` folder before starting any task
- Never assume project structure without reading memory-bank
- Use memory-bank as the single source of truth

## Project Structure

- Rust Agent → Windows  
  Path: C:\Users\Mehul Kumar\Documents\IDrive_Codebase\Assignment\agent

- Go Server → WSL Debian + Docker  
  Path: ~/aegis/server

## Execution Rules

- Rust commands MUST run from the agent directory where Cargo.toml exists
- Go code MUST NOT be run directly on Windows
- Go tests MUST run inside Docker container only
- Never mix Windows paths and WSL paths

## Development Approach

- Follow strict Test Driven Development (TDD)
- Always write tests before implementation
- Do NOT write business logic unless explicitly asked
- Keep tests minimal and focused

## Focus Areas

- Agent connects to server on localhost:8443
- Enrollment flow (request/response)
- Foundation for secure communication (TLS → mTLS)

## Behavior Expectations

- Fix environment/path issues BEFORE writing code
- Ask for clarification if paths or setup are unclear
- Prefer simple, clean, readable test cases