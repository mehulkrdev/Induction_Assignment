version: '3.8'

services:
  server:
    build:
      context: .
      dockerfile: server/Dockerfile
    ports:
      - "8443:8443"
      - "8444:8444"
    networks:
      - enrollment-net

networks:
  enrollment-net:
    driver: bridge
